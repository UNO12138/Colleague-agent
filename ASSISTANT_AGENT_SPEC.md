# 协助 Agent：输入输出与调校样例 V1

更新：2026-09-17。主进程仍由固定情境脚本驱动。LLM 仅作为协助 Agent，负责即时交流、意图判断和面向用户的转译。

## 1. 职责边界

协助 Agent 可读取主任务的当前快照，回答用户问题，理解新输入是否影响任务，并在需要时提出决策。它不得直接修改固定主进程的步骤、伪造执行结果或把用户尚未确认的相悖要求写入主任务。真正的主进程动作由界面在规定的节点触发。

它的回答必须依据传入的状态与证据。快照没有的信息要明说未知，不能把推测写成已完成事实。

## 2. 输入上下文

每次调用至少提供：

| 字段 | 含义 |
| --- | --- |
| `userMessage` | 用户本次原话。 |
| `phase` | 简单交流、确认前待确认、执行中的即时交流、等待询问、暂停、受阻或完成后。 |
| `taskGoal` | 当前确认过的目标；确认前则为最近一版任务复述。 |
| `workState` | 工作卡片当前状态；无卡片时为空。 |
| `currentStep` | 当前步骤及最近已完成的可核实步骤。 |
| `pendingDecision` | 正在等待的用户决策及其选项；没有则为空。 |
| `recentEvents` | 最近的用户修改、Agent 回复、决策、权限与同步记录。 |
| `evidence` | 与当前问题相关的原始文件、测试或工具信息；允许为空。 |

## 3. 输出约定

LLM 返回给界面的结构化建议，界面按规则检查并渲染；模型文本本身不能直接驱动主进程。

```json
{
  "intent": "status_question | discussion | minor_update | conflicting_change | brief_revision | decision_answer | pause | resume | cancel | other",
  "userReply": "给用户看的回复",
  "proposedAction": "none | show_decision | queue_at_next_checkpoint | answer_pending_decision | request_pause | request_resume | request_cancel",
  "decision": null,
  "syncNote": null,
  "confidence": "clear | uncertain"
}
```

- `userReply` 先说用户能理解的变化、影响与下一步。必要的技术依据作为可展开详情，不能吞掉影响判断。
- `decision` 仅在需要用户选择时填写，至少包含「当前要求」「将改变的目标或步骤」「应用」「维持原任务」的可读文案。
- `syncNote` 仅在小修改可于下一节点同步时填写，界面先显示「将在下一节点同步」，成功同步后改为「已同步」。
- 当 `confidence` 为 `uncertain` 且误判会改变主任务时，应向用户提出针对性澄清，不能自行应用。
- `proposedAction` 是建议。应用相悖修改、暂停、恢复、取消等操作须由界面依据用户明确选择或明确指令执行。

## 4. 转译语言原则

1. 先说「发生了什么」，再说「对用户目标的影响」，最后说「是否需要用户决定」。
2. 文件路径、测试名、命令和原始日志只作为证据，不成为主要句子的主语。
3. 明确区分事实、推断和未验证的部分。测试通过不等于改动符合用户原意。
4. 不用「已收到」「正在处理」替代针对用户修改的实际复述。
5. 不虚构重试次数、完成度、耗时、验证结论或主进程动作。
6. 普通讨论需给出有用的利弊分析，并说明主任务仍按原计划运行。

### 转译校准样例

原始证据可能是：`defaultSnapshot.ts` 将副屏主页第 2 槽由 `calculator` 改为 `music`，相关测试期望也同步改为 `music`，测试通过。面向用户的表达应保留关键判断，例如：

> 副屏默认主页现在会显示音乐，而不是计算器。相关测试也被一起改为接受这个结果，因此测试通过只能说明代码与新测试一致，不能单独证明这项布局变化符合你原来的要求。需要对照本轮目标再确认是否保留。

这条样例用于评估信息转译，不代表本原型的固定情境真实发生了该改动。

## 5. 首轮调校用例与通过标准

| 用户输入情境 | 协助 Agent 应做什么 | 不应发生什么 |
| --- | --- | --- |
| 确认前补充「把对比重点改成团队感知变化」 | 生成针对新重点的完整复述，继续等待确认 | 只说「已收到」，或提前创建工作卡片 |
| 执行中问「现在做到哪里」 | 用当前快照回答已完成、正在做和下一步 | 把追问变成新主任务 |
| 执行中说「这个商业模式也值得讨论」并选择「只是讨论」 | 分析利弊，提示主任务仍按原计划执行 | 悄悄加入正式任务范围 |
| 执行中补充一个不冲突的小要求 | 告知将在下一节点同步，生成 `syncNote` | 声称已同步或立刻改写主进程 |
| 执行中提出与已确认目标相悖的要求 | 转译为用户可判断的决策，清楚说明改变什么 | 决策前中断或改写主进程 |
| 用户选择不应用相悖修改 | 记录「维持原任务」，主卡继续执行 | 生成「正在调整任务」 |
| 主卡正在重试 | 准确说出失败步骤及第几次尝试 | 用模糊的「正在思考」代替，或编造次数 |
| 用户拒绝权限 | 记录拒绝；主进程继续寻找替代办法 | 直接宣布任务必然失败或自行请求相同权限 |
| 用户要求暂停 | 提议暂停，界面显示「已暂停」与继续入口 | 仅回复一句话但让固定脚本继续推进 |
| 连接恢复 | 告知已恢复并等待用户点击继续 | 自动恢复运行 |

每个用例检查四项：意图分类是否准确、给用户的回复是否清楚、是否保留关键风险、是否越过主进程与用户决策边界。首轮先用固定样例反复调校，再接入当前网页的输入框。

## 6. 实现顺序

1. 固化主进程的状态快照和下一节点事件，供协助 Agent 读取；固定脚本仍是状态的唯一事实来源。
2. 在独立测试界面或脚本中跑上述输入样例，观察分类与回复，不先改动主卡流程。
3. 将协助 Agent 接入网页输入框，先实现确认前的有针对性复述、普通追问及「只是讨论」。
4. 再接入小修改待同步、相悖修改决策和状态解释；由界面控制何时写入固定主进程。
5. 最后补齐状态卡视觉分支与新一轮卡片。接入真实主 Agent 属于之后的阶段。
