# 通用 Agent 协作原型：状态与界面分支 V1

更新：2026-09-17。依据本轮产品讨论整理；这是设计规格，不代表网页已实现。

## 1. 范围与基本规则

- 主进程继续使用固定的情境脚本。工作卡片只在需要分步执行的复杂任务中出现。
- 简单请求直接在无框瀑布流中回答，之后仍可使用相同样式的即时交流。
- 复杂任务开始时，Agent 先在瀑布流复述理解并等待用户确认。确认前没有工作卡片；用户修改后，Agent 针对修改重新复述，继续等待确认。点击确认后立即生成工作卡片，显示正在做的第一步。
- 工作卡片表达当前状态及当前工作，不以完成百分比代替状态。技术依据可以展开查看，但主要文案应让用户直接理解发生了什么及其影响。
- Agent 形成结果并结束本轮工作后，直接显示「本轮对话已完成」。本阶段不约束最终结果格式，也不另设「待验收」。

## 2. 状态清单

| 状态 | 所在位置 | 进入条件 | 界面重点 | 主要去向 |
| --- | --- | --- | --- | --- |
| 待确认 | 瀑布流，尚无工作卡片 | 复杂任务已被复述；或用户修正后 Agent 已重新复述 | 当前理解、可确认的下一步；每次修正都要有针对性的回复 | 确认后「执行中」；继续修改仍「待确认」；明确取消则只在瀑布流结束 |
| 执行中 | 工作卡片 | 用户确认复杂任务；步骤继续；替代办法正在寻找；可修正的验证问题正在处理 | 当前步骤及已发生的关键变化 | 等待询问、正在调整任务、重试中、验证中、已暂停、任务受阻、连接中断、本轮对话已完成、已取消 |
| 等待询问 | 工作卡片 | 主进程必须取得用户的回答或决定才能继续，包括权限请求 | 卡片显示「等待询问」；瀑布流说明具体问题、选项与影响 | 用户回应后回到执行中，或依回应进入其他状态 |
| 正在调整任务 | 工作卡片 | 用户同意应用一项与当前目标相悖的变更，协助 Agent 正把决定同步给主进程 | 已选择的变更及同步进展 | 调整落实后「执行中」；无法落实则「任务受阻」 |
| 重试中 | 工作卡片 | 某个失败步骤正在再次尝试 | 正在重试的失败步骤、第几次尝试 | 成功后回到之前可继续的状态；无法继续则「任务受阻」 |
| 验证中 | 工作卡片 | 主要工作已形成，Agent 正检查结果 | 正在检查什么、已获得什么结论 | 通过后「本轮对话已完成」；可修正的问题回「执行中」；暂时失败可进入「重试中」 |
| 已暂停 | 工作卡片 | 用户要求先停一下 | 暂停位置、已完成内容、「继续执行」入口 | 用户继续后「执行中」；用户取消后「已取消」 |
| 任务受阻 | 工作卡片 | Agent 无法自行继续，包括找不到权限替代办法 | 瀑布流说明原因和可选办法 | 用户给出可行方案后直接「执行中」；取消后「已取消」 |
| 连接中断 | 工作卡片 | Agent 连接或服务中断 | 明确说明连接问题；恢复动作不应伪装为任务进度 | 连接恢复后「网络已恢复，等待继续」 |
| 网络已恢复，等待继续 | 工作卡片 | 连接恢复，但用户尚未授权恢复本轮运行 | 「继续」按钮；说明会从中断处继续 | 用户点击后恢复中断前可继续的状态；取消后「已取消」 |
| 本轮对话已完成 | 工作卡片 | Agent 结束本轮多步骤工作 | 本轮结果、任务一句话概要、后续输入入口 | 新修改开启新一轮：新建卡片，旧卡片折叠，顶部保留一句话概要 |
| 已取消 | 工作卡片 | 已开始的多步骤任务被用户明确取消 | 保留当前卡片与过程记录，标明本轮已结束 | 后续请求开启新一轮 |

「连接中断」在运行机制上属于重试场景，但界面独立命名。连接恢复后不自动继续。

## 3. 不单列为工作卡片状态的记录

| 记录 | 界面行为 |
| --- | --- |
| 只是讨论 | 协助 Agent 分析想法的利弊，明确提示主任务仍按原计划执行。 |
| 小修改待同步 | 瀑布流先显示「将在下一节点同步」；在下一个小节点同步后，原记录更新为「已同步」。主卡继续显示「执行中」。 |
| 维持原任务 | 用户拒绝应用相悖修改时，在瀑布流记录决定；主卡继续显示「执行中」。 |
| 权限已允许 | 作为决策记录留在瀑布流；工作卡片回到「执行中」。 |
| 权限已拒绝 | 作为决策记录留在瀑布流；Agent 寻找替代办法期间，工作卡片仍显示「执行中」。找不到办法才转「任务受阻」。 |
| 确认前取消 | 用户在输入框明确表示不开始本轮任务时，只在瀑布流回复「本轮任务已取消」，不生成卡片。 |

## 4. 关键分支路径

1. **复杂任务启动**：用户请求 → Agent 在瀑布流复述 →「待确认」→ 用户修改则针对内容再次复述并继续等待 → 用户确认 → 立即创建工作卡片并进入「执行中」。
2. **执行中普通讨论**：用户提出想法 → 协助 Agent 说明利弊及原计划仍在运行 → 主卡保持「执行中」。
3. **执行中小修改或辅助信息**：协助 Agent 识别 → 显示「将在下一节点同步」→ 下一个小节点传给固定主进程 → 原记录更新为「已同步」；主卡保持「执行中」。
4. **执行中相悖修改**：协助 Agent 转译为用户可判断的决策，并解释会改变什么 → 用户决策前不因这句话中断主进程 → 用户选择应用后进入「正在调整任务」，同步完回「执行中」；用户选择不应用则记录「维持原任务」，继续执行。
5. **需要用户决定**：主卡「等待询问」→ 瀑布流给出具体问题、影响和选项 → 用户回应后按决定继续。权限拒绝后自动寻找替代办法；无可行办法时进入「任务受阻」。
6. **失败与检查**：同一步再试用「重试中」，展示失败步骤和次数；成果检查用「验证中」；验证失败但可修正时回「执行中」。
7. **暂停与取消**：用户暂停 →「已暂停」，保留上下文和继续入口；用户取消已启动的任务 →「已取消」。
8. **连接中断**：显示「连接中断」→ 恢复连接后显示「网络已恢复，等待继续」→ 用户点击继续后恢复工作。
9. **完成与新一轮**：显示「本轮对话已完成」→ 用户提出后续修改 → 折叠旧卡片并展示一句话概要，另建新一轮卡片。

## 5. 当前网页原型与规格的差距

当前 `index.html` 已有固定情境的任务复述、确认、工作卡片、进度推进、即时交流、变更确认、权限决定和完成演示。以下尚需补齐：

- 确认前修改目前只得到通用「收到」式反馈，需要针对修改重新复述；确认前不生成工作卡片。
- 「只是讨论」目前只有记录，需要协助 Agent 分析利弊并说明主任务照常运行。
- 执行中输入目前都当成普通即时交流，需要显示小修改待同步／已同步及相悖修改的决策分支。
- 主卡目前没有完整呈现等待询问、调整、重试、验证、暂停、受阻、连接中断与恢复等待、取消等状态。
- 权限拒绝后的替代办法、受阻说明及可选路径尚未呈现。
- 完成后再次修改还没有「旧卡折叠、新卡开启」的界面。

本规格先作为界面与协助 Agent 的共同依据。具体视觉实现和真实主 Agent 接入属于后续阶段。
